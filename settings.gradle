rootProject.name = 'amaranth'

// Enable type-safe project accessors
enableFeaturePreview('TYPESAFE_PROJECT_ACCESSORS')

// Enable version catalogs
dependencyResolutionManagement {
    versionCatalogs {
        libs {}
    }
}
